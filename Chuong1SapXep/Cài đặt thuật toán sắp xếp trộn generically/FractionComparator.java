import java.util.Comparator;

public class FractionComparator implements Comparator<Fraction> {

    /**
     * So sánh hai phân số f1 và f2
     *
     * @param f1 phân số thứ nhất
     * @param f2 phân số thứ hai
     * @return số âm nếu f1 < f2,
     *         0 nếu f1 = f2,
     *         số dương nếu f1 > f2
     */
    @Override
    public int compare(Fraction f1, Fraction f2) {
        // TODO
        return 0;
    }
}